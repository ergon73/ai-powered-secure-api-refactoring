# Flask Security Refactoring Project

[🇷🇺 Русский](README.md) | [🇬🇧 English](README.en.md)

![Python](https://img.shields.io/badge/python-3.11+-blue.svg)
![Flask](https://img.shields.io/badge/flask-3.0.0-green.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

## 📋 Обзор Проекта

Production-ready Flask API, демонстрирующий лучшие практики безопасности и современную разработку на Python. Проект был отрефакторен из уязвимого legacy-кода в безопасное, поддерживаемое и хорошо документированное приложение.

## 🔒 Улучшения Безопасности

### До (Найденные Уязвимости)

| Проблема | Критичность | Расположение |
|----------|-------------|--------------|
| SQL-инъекции | 🔴 Критично | `api.py`, `utils.py` |
| Пароли в открытом виде | 🔴 Критично | `utils.py:store_password()` |
| Состояния гонки | 🟡 Высокая | `utils.py:active_users` |
| Утечки ресурсов | 🟡 Высокая | Незакрытые соединения БД |
| Отсутствие type hints | 🟢 Средняя | Все файлы |
| Нарушения PEP 8 | 🟢 Средняя | Все файлы |

### После (Исправлено)

✅ **Параметризованные SQL-запросы** предотвращают атаки через инъекции  
✅ **Хеширование паролей PBKDF2** с солью (Werkzeug)  
✅ **Потокобезопасные операции** с блокировками  
✅ **Контекстные менеджеры** для управления ресурсами  
✅ **Полные type hints** (PEP 484)  
✅ **Соответствие PEP 8** во всем коде  
✅ **Правильные HTTP статус-коды** (200, 201, 404, 400, 500)  
✅ **Модульная архитектура** с разделением ответственности  

## 🏗️ Архитектура

```
Модульное Flask Приложение
├── app/               # Пакет приложения
│   ├── __init__.py    # Flask app factory
│   ├── database.py    # Управление соединениями БД
│   ├── routes.py       # API эндпоинты (blueprints)
│   ├── services.py    # Слой бизнес-логики
│   └── security.py    # Хеширование паролей и валидация
├── main.py            # Точка входа приложения
├── requirements.txt   # Python зависимости
├── Dockerfile         # Конфигурация контейнера
├── openapi.yaml       # Документация API
└── README.md          # Документация проекта
```

### Принципы Проектирования

- **Разделение ответственности**: Routes обрабатывают только HTTP, services содержат бизнес-логику
- **Безопасность прежде всего**: Все SQL-запросы используют параметризованные операторы
- **Управление ресурсами**: Контекстные менеджеры обеспечивают правильную очистку
- **Потокобезопасность**: Общее изменяемое состояние защищено блокировками
- **Типобезопасность**: Полные type hints для лучшей поддерживаемости

## 🚀 Быстрый Старт

### Локальная Разработка

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Запустить приложение
python main.py
```

API будет доступен по адресу `http://localhost:8000`

### Развертывание в Docker

```bash
# Собрать образ
docker build -t flask-security-api:latest .

# Запустить контейнер
docker run -p 8000:8000 flask-security-api:latest

# Проверить health endpoint
curl http://localhost:8000/health
```

## 📡 API Эндпоинты

| Метод | Эндпоинт | Описание | Статус-коды |
|-------|----------|----------|-------------|
| POST | `/users` | Создать нового пользователя | 201, 400, 500 |
| GET | `/users/{id}` | Получить пользователя по ID | 200, 404 |
| GET | `/health` | Проверка работоспособности | 200 |

Полная документация API: `openapi.yaml`

### Примеры Запросов

**Создать пользователя:**
```bash
curl -X POST http://localhost:8000/users \
  -H "Content-Type: application/json" \
  -d '{"name": "Алиса"}'
```

**Получить пользователя:**
```bash
curl http://localhost:8000/users/1
```

**Проверка работоспособности:**
```bash
curl http://localhost:8000/health
```

## 🧪 Тестирование

### Ручное Тестирование

```bash
# Создать пользователя
curl -X POST http://localhost:8000/users \
  -H "Content-Type: application/json" \
  -d '{"name": "Тестовый Пользователь"}'

# Получить пользователя (замените 1 на реальный ID)
curl http://localhost:8000/users/1

# Тест обработки ошибок - неверный запрос
curl -X POST http://localhost:8000/users \
  -H "Content-Type: application/json" \
  -d '{}'

# Тест обработки ошибок - не найдено
curl http://localhost:8000/users/999
```

### Использование Postman/Insomnia

Импортируйте файл `openapi.yaml` для получения всех эндпоинтов с предварительной конфигурацией.

## 📦 Технологический Стек

- **Фреймворк:** Flask 3.0.0
- **WSGI сервер:** Gunicorn 21.2.0
- **База данных:** SQLite (для production рекомендуется PostgreSQL/MySQL)
- **Безопасность:** Хеширование паролей Werkzeug (PBKDF2-SHA256)
- **Python:** 3.11+

## 🔐 Функции Безопасности

1. **Защита от SQL-инъекций**
   - Все запросы используют параметризованные операторы с плейсхолдерами `?`
   - Отсутствие форматирования строк или конкатенации в SQL

2. **Безопасность паролей**
   - PBKDF2 с SHA-256 и солью
   - Отсутствие хранения в открытом виде или обратимого "шифрования"

3. **Потокобезопасность**
   - Общее изменяемое состояние защищено `threading.Lock()`
   - Отсутствие состояний гонки в параллельных операциях

4. **Управление ресурсами**
   - Контекстные менеджеры для соединений с базой данных
   - Автоматическая очистка при ошибках

5. **Валидация входных данных**
   - Валидация запросов в обработчиках маршрутов
   - Проверка типов и санитизация

## 🐳 Конфигурация Docker

Dockerfile следует лучшим практикам для production:

- ✅ Легковесный базовый образ (`python:3.11-slim`)
- ✅ Пользователь без root-прав для безопасности
- ✅ Оптимизация кеширования слоев
- ✅ Production сервер Gunicorn (не Flask dev server)
- ✅ Health check для оркестрации контейнеров
- ✅ Правильная обработка сигналов

## 📚 Качество Кода

- **Type Hints:** Все функции имеют полные аннотации типов (PEP 484)
- **PEP 8:** Строгое соответствие руководству по стилю Python
- **Docstrings:** Документация в стиле Google для всех публичных функций
- **Обработка ошибок:** Специфичная обработка исключений с правильными HTTP статус-кодами
- **Архитектура:** Модульный дизайн с четким разделением ответственности

## 🤖 Разработка с Помощью ИИ

Этот проект был отрефакторен с использованием инструментов на основе ИИ (Cursor IDE) в соответствии с лучшими практиками:
- Инженерия промптов
- Стандарты качества кода
- Разработка с приоритетом безопасности
- Современные паттерны Python

## 📄 Лицензия

MIT License - см. файл LICENSE

## 👤 Автор

**Георгий Белянин (Georgy Belyanin)**  
📧 georgy.belyanin@gmail.com

Портфолио проект - Демонстрация рефакторинга безопасности

---

**Примечание:** Это демонстрационный проект. Для использования в production рекомендуется:
- Использовать PostgreSQL или MySQL вместо SQLite
- Добавить аутентификацию и авторизацию
- Реализовать rate limiting
- Добавить комплексный набор тестов
- Настроить CI/CD pipeline
- Использовать переменные окружения для конфигурации
- Добавить логирование и мониторинг
